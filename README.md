### Requirements

We need [Node.js](https://nodejs.org) to install and run scripts.

## Setup
- `npm install`

## Running the Client

- `npm start`
- http://localhost:8000

## Running the Server

- `node server/index.js`